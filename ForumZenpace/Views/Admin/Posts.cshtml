@model IEnumerable<ForumZenpace.Models.Post>
@{
    ViewData["Title"] = "Quan ly bai viet";
}

<div class="admin-shell animated-entry">
    <div class="admin-header">
        <div>
            <h1 class="admin-title">Noi dung thao luan</h1>
            <p class="admin-subtitle">Bang bai viet da duoc toi uu lai padding, kich thuoc chu va action de dat mat do thong tin hop ly hon.</p>
        </div>

        <a asp-action="Dashboard" class="btn btn-outline">
            <i class="fa-solid fa-arrow-left"></i> Quay lai
        </a>
    </div>

    <div class="surface admin-table-shell">
        <div class="scroll-x">
            <table class="table">
                <thead>
                    <tr>
                        <th>Tieu de</th>
                        <th>Tac gia</th>
                        <th>Chuyen muc</th>
                        <th>Trang thai</th>
                        <th>Ngay dang</th>
                        <th>Luot xem</th>
                        <th>Hanh dong</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var post in Model.OrderByDescending(p => p.CreatedAt))
                    {
                        <tr>
                            <td>
                                <a asp-controller="Post" asp-action="Details" asp-route-id="@post.Id" target="_blank" class="table-link">@post.Title</a>
                            </td>
                            <td>@post.User.FullName</td>
                            <td><span class="badge-tag">@post.Category.Name</span></td>
                            <td>
                                <span class="status-pill @(post.Status == "Active" ? "is-live" : "is-muted")">
                                    <span class="dot" style="background: currentColor;"></span>
                                    @(post.Status == "Active" ? "Cong khai" : "Da an")
                                </span>
                            </td>
                            <td class="table-note">@post.CreatedAt.ToString("dd MMM yyyy")</td>
                            <td>@post.ViewCount</td>
                            <td>
                                <div class="admin-actions">
                                    <form asp-action="TogglePostStatus" method="post" class="inline-form">
                                        <input type="hidden" name="id" value="@post.Id" />
                                        <button type="submit" class="btn btn-outline">
                                            @(post.Status == "Active" ? "An bai" : "Khoi phuc")
                                        </button>
                                    </form>

                                    <form asp-action="DeletePost" method="post" class="inline-form" onsubmit="return confirm('Ban co chac chan muon xoa bai viet nay?');">
                                        <input type="hidden" name="id" value="@post.Id" />
                                        <button type="submit" class="btn btn-danger-soft">
                                            <i class="fa-solid fa-trash-can"></i>
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
