@model ForumZenpace.Models.ForgotPasswordViewModel
@{
    ViewData["Title"] = "Quen mat khau";
}

<div class="auth-shell animated-entry">
    <div class="surface auth-card">
        <div class="auth-header">
            <div class="auth-mark">
                <i class="fa-solid fa-key"></i>
            </div>
            <h1 class="auth-title">Ho tro dat lai mat khau</h1>
            <p class="auth-subtitle">Nhap ten tai khoan hoac email da dang ky. Neu thong tin hop le, he thong se gui OTP dat lai mat khau.</p>
        </div>

        @if (TempData["AuthSuccessMessage"] is string authSuccessMessage)
        {
            <div class="auth-inline-alert mb-3">
                <i class="fa-solid fa-circle-check"></i>
                <span>@authSuccessMessage</span>
            </div>
        }

        @if (TempData["AuthErrorMessage"] is string authErrorMessage)
        {
            <div class="auth-inline-alert is-error mb-3">
                <i class="fa-solid fa-circle-exclamation"></i>
                <span>@authErrorMessage</span>
            </div>
        }

        <form asp-action="ForgotPassword" method="post" class="auth-form">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-2 text-center"></div>

            <div class="field">
                <label asp-for="Identifier" class="field-label">Ten tai khoan hoac email</label>
                <div class="input-icon">
                    <i class="fa-solid fa-envelope"></i>
                    <input asp-for="Identifier" class="form-control" placeholder="username hoac email" />
                </div>
                <span asp-validation-for="Identifier" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-primary" style="width: 100%;">Gui OTP dat lai mat khau</button>

            <div class="auth-footer">
                Muon quay lai?
                <a asp-action="Login">Trang dang nhap</a>
            </div>
        </form>
    </div>
</div>
